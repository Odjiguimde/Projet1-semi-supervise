# ─────────────────────────────────────────────────────────────────────────────
# requirements.txt — MNIST : Classification vs Deep Clustering (IDEC)
# Compatibilité : Python 3.9 – 3.12
# Installation  : pip install -r requirements.txt
# ─────────────────────────────────────────────────────────────────────────────

# ── Interface Streamlit
streamlit>=1.35.0

# ── Deep Learning — AUSSI utilisé pour charger MNIST (source 1, la plus fiable)
torch>=2.1.0
torchvision>=0.16.0

# ── Machine Learning classique
scikit-learn>=1.4.0

# ── Données
numpy>=1.26.0
pandas>=2.1.0

# ── Visualisation
matplotlib>=3.8.0
seaborn>=0.13.0

# ── Chargement MNIST — openml en dernier recours uniquement
# torchvision (ci-dessus) est la source principale sur Streamlit Cloud
# openml est optionnel (3ème fallback)
# openml>=0.14.0

# ─────────────────────────────────────────────────────────────────────────────
# Stratégie de chargement MNIST (automatique, sans intervention) :
#   1. torchvision.datasets.MNIST  ← prioritaire, fonctionne sur Streamlit Cloud
#   2. keras.datasets.mnist        ← si tensorflow installé
#   3. sklearn fetch_openml        ← dernier recours (peut échouer en cloud)
# ─────────────────────────────────────────────────────────────────────────────

# ── NOTE GPU (optionnel) :
# Pour CUDA, remplacer torch+torchvision par :
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
